/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['apod.nasa.gov', 'epic.gsfc.nasa.gov', 'mars.nasa.gov', 'www.youtube.com'],
  },
};

export default nextConfig;
